# Project Rules

- **Root vs Web Files:** When modifying the main HTML interface (`index.html`) or its core logic (`app.js`), you MUST always modify the ones located in the project's root directory. Do NOT modify the copies located in `web/public/` unless explicitly requested, as the live application serves the interface from the root directory files.
